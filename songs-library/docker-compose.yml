services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8100:8100"
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_URL: sqlite:////app/data/library.db
