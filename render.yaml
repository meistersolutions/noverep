# Render Blueprint — free tier deploy (backend + static frontend)
# Docs: docs/DEPLOY_FREE.md
#
# Prerequisites:
#   1. Code pushed to GitHub (e.g. meistersolutions/noverep)
#   2. Neon Postgres database created (free) — paste DATABASE_URL in Render dashboard
#
# Deploy: Render Dashboard → New → Blueprint → connect this repo

services:
  - type: web
    name: noverep-api
    runtime: docker
    region: oregon
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://noverep.onrender.com
      - key: REDIS_URL
        value: redis://localhost:6379/0

  - type: web
    name: noverep
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    rootDir: frontend
    envVars:
      - key: VITE_API_URL
        value: https://noverep-api.onrender.com/api/v1
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
